-- Buyunaihe AI Agent results database (PostgreSQL, isolated from main business DB)
-- Tables are created automatically by agent-migrate on startup via SQLAlchemy
-- Base.metadata.create_all(). This script is a placeholder for any manual seeding.

-- (no seed data needed; agent-migrate creates ai_suggestions + ai_audit_reports)
